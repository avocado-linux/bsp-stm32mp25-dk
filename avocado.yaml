default_target: stm32mp25-dk
supported_targets:
  - stm32mp25-dk

extensions:
  avocado-bsp-stm32mp25-dk:
    version: 2024.1.0
    release: r0
    summary: Board support for the STM32MP25 Discovery Kit (STM32MP257F-DK)
    description: Board support for the STM32MP25 Discovery Kit (STM32MP257F-DK)
    license: Apache-2.0
    url: https://github.com/avocadolinux/avocado-os
    vendor: Avocado Linux <info@avocadolinux.org>

    types:
      - sysext
      - confext

    on_merge:
      - udevadm control --reload
      - udevadm trigger --type=subsystems --action=add --settle
      - udevadm trigger --type=devices --action=add --settle

    packages:
      # Bring-up: ship every kernel module via the kernel-modules meta-package.
      # Trim down to the lsmod set once the board boots cleanly.
      kernel-modules: '*'
      linux-firmware-bcm43xx: '*'

sdk:
  image: docker.io/avocadolinux/sdk:{{ env.AVOCADO_DISTRO_RELEASE }}-{{ env.AVOCADO_DISTRO_CHANNEL }}
